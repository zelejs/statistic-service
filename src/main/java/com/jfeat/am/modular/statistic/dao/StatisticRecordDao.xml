<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.modular.statistic.dao.StatisticRecordDao">

	<select id="getStatisticRecordByTypeIdAndStartTimeAndEndTime" resultType="map">
	SELECT type_id,`group`,record_time
	<foreach collection="fields" index="index" item="field">
        ,max(case when field_name='${field}' then value else 0 end) as '${field}'
    </foreach>
    FROM st_statistic_record
    WHERE record_time BETWEEN #{startTime} AND #{endTime}
    GROUP BY type_id,`group`,record_time
    </select>

</mapper>
